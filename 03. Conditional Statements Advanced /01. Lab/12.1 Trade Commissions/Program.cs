//Input
string city = Console.ReadLine();
double sels = double.Parse(Console.ReadLine());

//Calculations
if (city == "Sofia")
{
    double commission = 0.00;
    switch (sels)
    {
        case (sels >= 0 && sels <= 500):
            commission = sels * 0.05; break;
        case (sels >500 && sels <= 1000):


    }
}

//Output
